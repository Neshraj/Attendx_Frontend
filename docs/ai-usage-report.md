# Mandatory AI Usage Report

> Fill this in with the exact prompts/actions you actually used before submission. Do not claim AI generated something if you did not use it.

## AI TOOL USED

ChatGPT / Cursor / GitHub Copilot / Other: ____________________

## WHAT I ASKED AI TO DO

1. Help break the vague attendance problem into users, workflows, permissions, edge cases and an MVP.
2. Review the proposed multi-tenant data model, API boundaries and validation strategy.
3. Generate or improve selected implementation pieces while I reviewed the code and tested the behavior.

## PROMPT THAT WAS MOST USEFUL

> Design a small but production-minded multi-tenant attendance SaaS for schools and colleges. Focus on tenant isolation, RBAC, attendance session uniqueness, correction approval workflow, audit history and low-attendance reporting. Keep the architecture appropriate for a MERN MVP and explain trade-offs.

## CODE GENERATED BY AI: What part?

Document the exact files/sections here.

Example:

- Initial React page scaffolding
- API boilerplate
- validation schema starter
- UI table/card structure

## CODE I MODIFIED: What part?

Document the exact changes made after reviewing generated code.

Example:

- tenant filters
- authorization checks
- duplicate-session handling
- error handling
- responsive UI details
- data mapping

## AI OUTPUT THAT WAS WRONG

Document at least one real mistake you found while testing.

Example candidates only — replace with what actually happened:

- AI trusted an `organizationId` coming from the browser.
- AI allowed a user to review a correction without checking the role on the server.
- AI forgot a duplicate constraint.
- AI produced a query that returned another tenant's data.

## HOW I IDENTIFIED THE PROBLEM

Describe the actual test that exposed it.

Example:

> I logged in as one institution user and changed the request payload to reference another institution's ID. The first implementation returned data outside the current tenant, so I traced the query path and found that the tenant filter was using request data instead of the authenticated user context.

## HOW I FIXED IT

Describe your actual fix.

Example:

> I moved tenant resolution to authentication context and applied `organizationId: req.user.organizationId` at the database query boundary. I then repeated the cross-tenant test and confirmed that the other institution's records were no longer returned.
